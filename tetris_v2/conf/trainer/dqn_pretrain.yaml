name: dqn_pretrain
args:
  dataset_dir: null
  extra_dataset_dir: []
  updates: 20000
  batch_size: 512
  learning_rate: 0.001
  hidden_sizes: [512, 256]
  lambda_bc: 1.0
  lambda_pair: 0.0
  pairs_per_sample: 0
  log_interval: 500
  checkpoint_frequency: 5000
  seed: 123
  device: null
  init_checkpoint: null
  log_dir: ${paths.runs_dir}/dqn_pretrain
