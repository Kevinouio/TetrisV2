# @package _global_
defaults:
  - override /trainer: ppo

preset:
  name: ppo_from_scratch
  trainer: ppo

trainer:
  args:
    total_timesteps: 1000000
    max_steps: 300
    log_dir: ${paths.runs_dir}/ppo_from_scratch
